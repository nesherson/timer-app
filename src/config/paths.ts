export const paths = {
    app: {
        root: {
            path: '/',
            getHref: () => '/',
        },
        timers: {
            path: '/',
            getHref: () => '/',
        }
    },
} as const;